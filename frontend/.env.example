VITE_API_PROXY_TARGET=http://localhost:5000

# Optional for production builds or when you do not want to use the Vite dev proxy.
# Leave empty in local Vite dev so /api and /uploads are proxied automatically.
VITE_API_BASE_URL=
