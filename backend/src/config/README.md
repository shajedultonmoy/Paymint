# Paymint Database Schemas and Migrations

This directory is intended to house all database-related files for MySQL, such as:
- SQL schema definitions (`.sql` files)
- Database migration scripts (if using a migration tool like `sequelize-cli`)